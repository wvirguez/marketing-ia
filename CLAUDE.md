@AGENTS.md

# Claude Code repository entry point

The imported `AGENTS.md` is the shared source of repository-wide rules for
Claude Code and Codex. Read `apps/web/CLAUDE.md` and `apps/web/AGENTS.md` before
frontend work. Keep frontend-specific instructions there. Any future `apps/api`
must follow the shared root rules and add its own scoped instructions when its
implementation is requested.
